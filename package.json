{
  "name": "leo-mod-more-tnt",
  "version": "1.0.5",
  "private": true,
  "description": "Leo's More TNT add-on for Minecraft Bedrock",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit && biome check .",
    "lint": "biome check .",
    "fix": "biome check --write .",
    "version": "node tools/sync-version.mjs && git add pack/manifest.json resource_pack/manifest.json",
    "build": "npm run check && node tools/sync-version.mjs && esbuild src/main.ts --bundle --format=esm --external:@minecraft/server --outfile=pack/scripts/main.js",
    "package": "npm run build && node tools/package-mcaddon.mjs",
    "icon": "node tools/make-icon.mjs",
    "textures": "node tools/make-textures.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.8",
    "@minecraft/server": "2.8.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
